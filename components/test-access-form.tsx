"use client";
import {useState} from 'react';
export function TestAccessForm(){const [code,setCode]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError('');const response=await fetch('/api/test-access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});if(response.ok){location.assign('/');return}const body=await response.json().catch(()=>null) as {error?:string}|null;setError(body?.error||'Test access could not be started.');setBusy(false)}return <form className="email-signin" onSubmit={submit}><label htmlFor="test-access-code">Test access code</label><div><input id="test-access-code" type="password" autoComplete="off" required value={code} onChange={event=>setCode(event.target.value)} placeholder="Enter the private test code"/><button className="primary" disabled={busy}>{busy?'Opening…':'Open test workspace'}</button></div>{error?<p className="signin-error" role="alert">{error}</p>:null}</form>}
